import os
from maquinadebusca.settings import BASE_DIR




class StopWords:

    def __init__(self, file):
        self.file_path = os.path.abspath(BASE_DIR+'/'+file)
        with words as
    

    def get(self):
        ...